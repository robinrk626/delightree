const GENDER = {
  MALE: 'Male',
  FEMALE: 'Female',
  OTHERS: 'Other',
};

const ORDER_STATUS = {
  PENDING: 'pending',
  COMPLETED: 'completed',
  CANCELED: 'canceled',
};

const PRODUCT_CATEGORIES = {
  ELECTRONICS: 'Electronics',
};

module.exports = {
  GENDER,
  ORDER_STATUS,
  PRODUCT_CATEGORIES,
};
